@model IEnumerable<Employee>
<div class = 'container'>

    <table class = "table table-bordered">
        <thead>
            <th>@Html.DisplayNameFor(d=>d.EmpId)</th>
            <th>@Html.DisplayNameFor(d=>d.Name)</th>
            <th>@Html.DisplayNameFor(d=>d.Salary)</th>
            <th>@Html.DisplayNameFor(d=>d.Email)</th>
            <th>@Html.DisplayNameFor(d=>d.Dob)</th>
            <th></th>
        </thead>
        <tbody>
        @foreach (var data in Model)
        {
            <tr>
                <td>@Html.DisplayFor(e=>data.EmpId)</td>
                <td>@Html.DisplayFor(e=>data.Name)</td>
                <td>@Html.DisplayFor(e=>data.Salary)</td>
                <td>@Html.DisplayFor(e=>data.Email)</td>
                <td>@Html.DisplayFor(e=>data.Dob)</td>
            </tr>
            
        }
    </tbody>
</table>

@* @model IEnumerable<Department>
<div class = 'container'>

    <table class = "table">
        <thead>
            <th>@Html.DisplayNameFor(d=>d.Id)</th>
            <th>@Html.DisplayNameFor(d=>d.DeptName)</th>
            <th>@Html.DisplayNameFor(d=>d.Location)</th>
            <th></th>
        </thead>
        <tbody>
            @foreach(var dept in @Model)
            {
                <tr>
                    <td>@dept.Id</td>
                    <td>@dept.DeptName</td>
                    <td>@dept.Location</td>
                    <td></td>
                    <td>
                        @Html.ActionLink("Details","Display",new {id=dept.Id}) &nbsp; &nbsp;
                        @Html.ActionLink("Edit","Edit",new {id=dept.Id}) &nbsp; &nbsp;
                        @Html.ActionLink("Delete","Delete",new {id=dept.Id}) &nbsp; &nbsp;
                    </td>
                </tr>
            }
        </tbody>
    </table>

    </div> *@